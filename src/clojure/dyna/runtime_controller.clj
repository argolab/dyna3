(ns dyna.runtime-controller
  (:require [dyna.utils :refer :all]))

;; might want to make the runtime controller as a Java interface s that it can be easily implemented by python methods
(defsimpleinterface IDynaRuntimeController

  )
