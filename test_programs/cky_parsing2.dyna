parse(I,K,T) max= parse(I,J,T1) * parse(J,K,T2) * rule(T1,T2,T) for I < J, J < K.
parse(I,I+1, T) max= rule(word(I), T).


length max= I for _ is word(I).

score = parse(0, length, "S").

:- import_csv rule/2 "word_mapping.csv".
:- import_csv rule/3 "non_term_mapping.csv".

word(0) = "Papa".
word(1) = "ate".
word(2) = "the"
word(3) = "caviar"
word(4) = "with"
word(5) = "a".
word(6) = "spoon".
word(7) = ".".